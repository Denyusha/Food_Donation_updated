{
  "name": "food-donation-app",
  "version": "1.0.0",
  "description": "Full-stack Food Donation Web Application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "food-donation",
    "waste-reduction",
    "social-impact"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "mongodb": "^7.1.0",
    "mongoose": "^9.2.1"
  }
}
