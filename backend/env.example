### Backend Environment Variables
### Rename this file to `.env` (or copy values into `backend/.env`)

# Server
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# MongoDB
# Atlas example:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-host>/<db-name>?retryWrites=true&w=majority
# Local example:
# MONGODB_URI=mongodb://localhost:27017/food_donation
MONGODB_URI=

# If your Atlas URI contains a "<password>" placeholder, set it here (optional)
MONGODB_PASSWORD=

# Auth
JWT_SECRET=
JWT_EXPIRE=7d

# Optional (used by some features/docs)
GOOGLE_MAPS_API_KEY=

